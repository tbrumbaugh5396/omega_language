# Stage 07 — Making it, and showing it
| Document | Goes to | When |
|---|---|---|
| `project-plan-checklist.md` | Internal | Your running sheet — update it weekly |
| `homepage-feedback-form.md` | **Client** | Round 1: the homepage direction |
| `website-feedback-form.md` | **Client** | Round 2: the full system |
| `change-order.md` | **Client** | Every single time something new comes up |
| `code-review.md` | **Client** | With round 1 — what "reviewed" means when we say it |
| `test-process.md` | **Client** | With the kickoff pack — what runs, when, and the rule that nothing ships red |
| `verification-process.md` | **Client** | Before round 1 — what a person checks and signs at each gate |
| `code-coverage.md` | **Client** | With each release note — the number, its floor, and what it does not mean |
| `types-of-testing.md` | **Client** | With the kickoff pack — the seven kinds, so a release note reads as intended |

**Two rounds, written into the price and the contract.** Each round is one
consolidated response within five working days. A third is a paid add-on. This
one clause is the difference between a profitable studio and free work forever.

**Use the change order every time, warmly.** It takes two minutes. The cost of
*not* using it is the whole margin, collected in small friendly increments.

**Quality is a document, not a promise.** The five quality documents above
say what review, testing, verification and coverage mean for this build, in
words a client can hold us to. Send them; then every release note reports
against them.

**Gate:** round 2 signed off and the final invoice raised.
