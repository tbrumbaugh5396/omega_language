# Runtime data — holds the admin key, SMTP credentials, TLS private keys,
# the live database and customer uploads. Never commit.
data/

# Local virtualenv (~100MB) and build output
.venv/
business-control.zip

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Piper TTS voice models — 60–110MB each, re-downloaded from Hugging Face
# by the video tooling; never versioned.
assets/video/voices/

# One supplied artwork is 216MB — too heavy for git without LFS. It lives in
# assets/ on the working machine; the seeder will say plainly if it's absent.
assets/All Flavors 02 without Background with Colored Lights.png
