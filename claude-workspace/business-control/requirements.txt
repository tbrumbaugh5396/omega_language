fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
qrcode>=7.4
pywebpush>=1.14
websockets>=12
python-liquid>=2.0
Pillow>=10.0
python-multipart>=0.0.9
fpdf2>=2.8
# Rows in Postgres (optional: only when the install's store is postgres, or
# BC_STORE=postgres). The SQLite face stays the default and needs nothing.
psycopg[binary]>=3.2
# Translating the shop without a server or a key (optional): Argos Translate
# runs in this process and downloads a model per language on first use.
# NLLB (200 languages) wants transformers + torch + sentencepiece on top.
# argostranslate>=1.9
